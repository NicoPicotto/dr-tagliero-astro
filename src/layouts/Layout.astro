---
import Navbar from "@/components/commons/navbar.astro";
import Footer from "@/components/commons/footer.astro";
import "@/styles/starwind.css";

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  canonical?: string;
  type?: "website" | "article";
  noindex?: boolean;
}

const SITE_URL = "https://matiastagliero.com";
const SITE_NAME = "Abog. Matías Tagliero";
const DEFAULT_DESCRIPTION =
  "Matías Tagliero – Abogado. Especializado en derecho civil, penal, laboral y familiar. Asesoría legal personalizada, elaboración de contratos y representación judicial en Argentina.";
const DEFAULT_OG_IMAGE = `${SITE_URL}/profile-pic.png`;

const {
  title,
  description = DEFAULT_DESCRIPTION,
  ogImage = DEFAULT_OG_IMAGE,
  canonical = `${SITE_URL}${Astro.url.pathname}`,
  type = "website",
  noindex = false,
} = Astro.props;

const pageTitle = title ? `${title} | ${SITE_NAME}` : SITE_NAME;
const isHomepage = Astro.url.pathname === "/";

const personSchema = {
  "@context": "https://schema.org",
  "@type": "Person",
  "@id": `${SITE_URL}/#matias`,
  name: "Matías Tagliero",
  jobTitle: "Abogado",
  alumniOf: "Universidad Nacional de Córdoba",
  email: "hola@matiastagliero.com",
  telephone: "+5493513884467",
  worksFor: { "@id": `${SITE_URL}/#legalservice` },
};

const legalServiceSchema = {
  "@context": "https://schema.org",
  "@type": ["LegalService", "LocalBusiness"],
  "@id": `${SITE_URL}/#legalservice`,
  name: SITE_NAME,
  url: SITE_URL,
  telephone: "+5493513884467",
  email: "hola@matiastagliero.com",
  image: `${SITE_URL}/profile-pic.png`,
  address: {
    "@type": "PostalAddress",
    streetAddress: "Saavedra 365",
    addressLocality: "General Deheza",
    addressRegion: "Córdoba",
    addressCountry: "AR",
  },
  areaServed: "Argentina",
  knowsAbout: [
    "Derecho Civil",
    "Derecho Penal",
    "Derecho Laboral",
    "Derecho Familiar",
    "Derecho Inmobiliario",
    "Sucesiones",
    "Jubilaciones",
  ],
  description: DEFAULT_DESCRIPTION,
};

const websiteSchema = {
  "@context": "https://schema.org",
  "@type": "WebSite",
  "@id": `${SITE_URL}/#website`,
  url: SITE_URL,
  name: SITE_NAME,
  inLanguage: "es-AR",
};
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Primary SEO -->
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <meta name="author" content="Matías Tagliero" />
    <meta
      name="keywords"
      content="abogado, Matías Tagliero, matrícula 2-2050, derecho civil, derecho penal, derecho laboral, derecho familiar, derecho inmobiliario, sucesiones, jubilaciones, asesoría legal, representación judicial, consultoría jurídica, contratos, litigios, General Deheza, Córdoba, Argentina"
    />
    <meta name="robots" content={noindex ? "noindex,nofollow" : "index,follow"} />
    <link rel="canonical" href={canonical} />

    <!-- Open Graph -->
    <meta property="og:type" content={type} />
    <meta property="og:url" content={canonical} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:locale" content="es_AR" />
    <meta property="og:site_name" content={SITE_NAME} />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="preload"
      as="style"
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap"
    />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap"
      media="print"
      onload="this.media='all'"
    />
    <noscript>
      <link
        rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap"
      />
    </noscript>

    <!-- Favicon & Generator -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

    <!-- Schema.org: LegalService + Person -->
    <script is:inline type="application/ld+json" set:html={JSON.stringify(legalServiceSchema)} />
    <script is:inline type="application/ld+json" set:html={JSON.stringify(personSchema)} />
    {isHomepage && (
      <script is:inline type="application/ld+json" set:html={JSON.stringify(websiteSchema)} />
    )}

    <!-- Additional head content injected by pages (e.g. Article schema) -->
    <slot name="head" />
  </head>
  <body>
    <Navbar />
    <main class="flex-grow">
      <slot />
    </main>
    <Footer />
  </body>
</html>

<style>
  html {
    scroll-behavior: smooth;
  }

  body {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
  }

  .flex-grow {
    flex: 1;
  }
</style>
